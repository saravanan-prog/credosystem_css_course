#  Attribute Selector

The attribute selector selects elements based on HTML attributes.

Example
```
input[type="text"] {
  border: 2px solid blue;
}
```

HTML:

```
<input type="text">
<input type="password">
```